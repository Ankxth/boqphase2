[pytest]
# Lets test modules do `from app.services...` / `from tests.golden_utils
# import ...` without each file needing its own sys.path.insert hack.
# Supported natively since pytest 7.0 -- run pytest from the backend/
# directory (the same directory this file lives in).
pythonpath = .
testpaths = tests